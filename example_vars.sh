#!/usr/bin/env bash

# set the below variables to your SMTP server details, and save the file as vars.sh

export SMTP_SERVER=<smtp_server>:<smtp_port>
export SMTP_USER=<smtp_user>
export SMTP_PASSWORD=<smtp_password>
